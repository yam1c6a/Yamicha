[project]
name = "yamicha"
version = "0.2.0"
description = "A persistent dialogue-continuity implementation for Yamicha"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
yamicha = "yamicha.__main__:main"

[tool.yamicha]
source-root = "src"
test-root = "tests"
