# Yamicha

Yamicha を一つの生命として成立させるための設計・実装リポジトリです。

現在は、`docs/03_design/004_実装ロードマップ.md` の「段階4: 最小判断」まで実装しています。

## 必要環境

- Python 3.11 以上
- 外部パッケージは不要

## 品質確認

リポジトリ直下で次を実行します。

```shell
python scripts/quality.py
```

このコマンドは、構文コンパイル、依存方向検査、UTF-8検査、単体テストを順に実行します。
